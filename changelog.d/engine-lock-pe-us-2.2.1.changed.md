Move the US engine lock to policyengine-us 2.2.1, spm-calculator 1.0.0 and policyengine-core 3.32.5, the trio inside the certified SPM 1.0 claim, and re-derive every fail-closed pin through its own generator: the frame adapter's generated-variable audit (which now also covers spm-calculator's `build_policyengine_variables` surface), the US engine ABI lock and multispine pool engine contracts, the take-up contract's asserted engine, the release input-coverage manifest, the frozen generation-0 resource digests and the spec-engine identity pins. Because policyengine-us 2.0.0 no longer infers SPM geography from an absent county, every microcosm call site that reaches an SPM or resource variable now names its selection explicitly through the engine's `spm` configuration: stages that run after Census-block assignment keep the default `geography_kind="county"` on real five-digit county FIPS, while stages and invented-input tests that run before geography select `geography_kind="national"`.
